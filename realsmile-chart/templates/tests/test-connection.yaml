apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "realsmile-chart.fullname" . }}-test-connection"
  labels:
    {{- include "realsmile-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "realsmile-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
